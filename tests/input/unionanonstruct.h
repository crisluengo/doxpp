/**
 * The union A.
 *
 * A longer description of A.
 */
union A
{
	struct {
		/** The a field. */
		float a;
		/** The b field. */
		float b;
	};

	struct {
		float x; /**< The x field. */
		float y; /**< The y field. */
	};
};
