/// \macro NAME
/// A macro.
/// Detailed documentation here.
#define NAME function

/// \def TYPE
/// Another macro.
#define TYPE int

/// A function called `function`.
TYPE NAME(TYPE x);

/// \macro FUNC(x, y)
/// A function macro.
#define FUNC(x, y) ((x)+(y))
